{
    "version": 2,
    "builds": [
      {
        "src": "next.config.js",
        "use": "@vercel/next",
        "config": { "buildCommand": "next build" }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/"
      }
    ]
  }